# sample config file
#zones

[rule]
	program = /usr/bin/ssh
	verdict = accept

[zones]
	local = 10.0.0.0/8
	internet = 0.0.0.0/0

# aliases for users
# root user's program will be asked on user user's GUI
[alias]
	root = user
